# 当前实现与下一步

状态：用户已确认更正后的接线代码，真实环境验收待完成；不再按旧 P1/P2/P3 的大骨架阶段推进。

## 已实现

- 同一原生 experimental Fully Async 入口，单一 profile，关闭时不依赖伴生包。
- GS 真实发现/获取、TaskRunner 双向句柄和结束解绑。
- 九类实际扩展及原生创建链，原生业务透传。
- 删除未参与当前初始化的协议、dummy 服务、复制框架与占位测试，不保留备份。

## 接下来只验证接入

1. 用户已审视并确认子仓接线代码与 verl 两处补丁；后续验收继续检查所有者和原生参数保留。
2. 开发者已通过 58 项配置/源码/接线检查和 3 项 CPU Ray 测试；源码与替身测试不证明完整原生初始化。
3. 用户在具备原生 verl/vLLM 依赖的环境执行真实父类检查。
4. 用户把同版源码部署到全部 Ray 节点，从原生入口执行训练，检查 GS、扩展 Actor、普通对象及原生生成/训练/参数同步。

这些检查没有完成前，不能宣称完整原生流程已经验证。特别是当前本地没有导入真实 verl/vLLM 父类或启动 GPU runtime。
当前配置选择 vLLM 非 PD、纯 STANDALONE；其他 backend/PD 不会偷偷回退原生类型。

## 后续功能不在本轮范围

接入验收后，用户再逐项决定心跳、空闲识别、捐赠/借用、动态实例、权重版本与调度功能。
本轮不为这些功能保留独立协议、状态机、空 gate 或大批未调用接口。
